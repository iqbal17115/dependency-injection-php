# PHP Dependency Injection Example

This project demonstrates **Dependency Injection (DI)** in PHP using multiple database connections (MySQL, PostgreSQL, Oracle) with **interface-based design**.

---

## **Project Structure**

